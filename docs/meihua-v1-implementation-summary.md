# Implementation summary

Meihua V1 is implemented on `feat/meihua-v1` with a deterministic calculation engine, browser-local question time provenance, a mobile-first evidence UI, Gemini interpretation constrained to frozen engine output, archive persistence, deterministic home placement, and WebKit E2E coverage. Main is intentionally unchanged pending preview review.
